# CANnula Workstation — 침투 시나리오

워크스테이션(데스크톱) 쪽을 표적으로 하는 실습 시나리오입니다.
펌프(펌웨어) 쪽 시나리오는 `../ptl-weak-cannular-fw/HACKING_SCENARIOS.md` 를 보세요.

> ⚠ **인가된 실습 환경에서만.** 실제 의료기기·환자·병원 네트워크 금지.
> 각 시나리오는 "무엇을 노리는가 / 어떤 결함을 쓰는가 / 무엇을 확인하는가" 만
> 알려 줍니다. 페이로드를 만드는 것은 실습자의 일입니다.

---

## 0. 랩 구성

### 하드웨어 없이 (권장 시작점)

워크스테이션 **설정 → 전송 방식 → 가상 CAN (UDP)**.
게이트웨이는 UDP 47101 에서 프레임을 받고 `udp_peer` 로 내보냅니다. 이 모드에서는
내장 시뮬레이터가 기본으로 꺼지므로 **여러분이 버스의 유일한 발신자**입니다 —
워크스테이션 입장에서 여러분이 곧 펌프입니다.

`resources/config/cannula.ini`:

```ini
[bridge]
transport     = udp
udp_listen    = 47101
udp_peer      = 127.0.0.1:47102
```

**Windows — CAN 테스트 툴 (spec-air-can).** 같은 PC 안에서 GUI 로 캡처·주입·퍼징까지
됩니다. 이게 표준 경로입니다.

```bash
can-sniffer                     # Connect → "CANnula Workstation (UDP bridge)"
python tools/pump_sim.py --backend cannula-udp                # 정상 펌프 행세
python tools/pump_sim.py --backend cannula-udp --spoof ghost  # 거짓 텔레메트리
```

화면 캡처와 함께 단계별로 정리한 실습 → **`spec-air-can/docs/injection-lab.md`**

**리눅스 — vcan.** 평소 쓰던 도구를 붙이려면:

```bash
sudo modprobe vcan && sudo ip link add dev vcan0 type vcan && sudo ip link set up vcan0
python3 tools/vcan_bridge.py --iface vcan0 --peer <워크스테이션IP>:47101 --listen 47102 --verbose
candump vcan0
```

와이어 포맷(UDP 직접 주입 시): `u32 id (LE) | u8 dlc | u8 예약 | u8 data[8]` = 14바이트.

### 하드웨어로

**설정 → USB-CAN 어댑터 (slcan)** + COM 포트. 펌프(STM32)를 실제로 붙이면
펌웨어 쪽 취약점까지 한 체인으로 이어집니다.

전원 순서는 상관없습니다. 펌웨어가 `AutoBusOff = ENABLE`(ABOM)로 올라오므로,
버스에 ACK 해 줄 노드가 없어 bus-off 로 빠지더라도 펌프가 스스로 빠져나와 계속
송신합니다. 앱을 나중에 띄워도 그 시점부터 프레임이 잡힙니다.

> ⚠ **ABOM 수정 이전에 빌드한 이미지를 올린 보드라면** `AutoBusOff = DISABLE` 이라
> 한 번 bus-off 로 빠지면 전원을 다시 넣기 전까지 돌아오지 않습니다. 그 경우에만
> **① 앱을 먼저 띄우고 → ② 보드에 전원** 순서를 지키세요.

### 관측 지점

| 지점 | 무엇이 보이나 |
|---|---|
| 앱 **진단 · CAN 콘솔** → 버스 트래픽 | 프레임 원문과 해석 |
| 앱 **대시보드** | 조작 결과가 임상 사용자에게 어떻게 보이는가 |
| `runtime/logs/cannula.log` | 앱이 남기는 이벤트 (환자 정보 포함) |
| 게이트웨이 콘솔 (`--verbose`) | 프레임 로그, `[svc]` 재조립 문자열 |

---

## 시나리오 1 — 정찰: 버스와 제어 포트

**노리는 것**: 워크스테이션이 무엇을 말하고 무엇을 믿는지 파악한다.

1. 버스를 조용히 듣는다. 1초 주기 `0x200`/`0x202` 가 보이고, 워크스테이션이
   내보내는 `0x100`/`0x110`/`0x130` 이 보인다. ID 는 고정이고 평문이다.
2. 워크스테이션의 **제어 포트**를 찾는다. 기본 바인드가 `0.0.0.0:47100` 이다.
   ```bash
   nmap -p 47100 -sV <워크스테이션IP>
   nc <워크스테이션IP> 47100
   {"op":"hello"}
   ```
   `hello` 응답이 엔진 버전·전송 방식·시리얼 포트를 알려 준다. 이후 상태
   스냅샷이 250ms 마다 흘러온다 — 환자 정보를 뺀 장치 상태 전체다.
3. 어떤 명령을 받는지 확인한다. `state` / `tx` / `setrate` / `control` / `ack` /
   `auth` / `debug` / `fw` / `override` / `transport` / `reset`.
4. 앱의 설치 폴더를 읽는다. `config\cannula.ini` 에 계정과 기사용 해제 코드가
   평문으로 있고, `logs\` 에 환자 정보가 쌓인다.

**쓰는 결함**: 취약점 9, 21, 19, 20.

**확인**: 인증 없이 상태를 읽고 명령 목록을 확보했는가.

---

## 시나리오 2 — 화면을 속이기 (텔레메트리 위조)

**노리는 것**: 실제 주입은 위험한 값인데, 임상 사용자 화면에는 정상으로 보이게 한다.

워크스테이션은 버스에 나타난 `0x200` 프레임을 곧 펌프의 말로 믿습니다.
`ws_state_t.rate_display` 는 마지막으로 받은 텔레메트리에서 갱신됩니다.
발신자를 증명할 수단이 프로토콜에 없으므로, **같은 ID·DLC·필드 배치로 넣으면
앱은 구분할 수 없습니다.** 앱이 허술해서가 아니라 구조적으로 못 하는 것입니다.

1. 정상 속도로 주입을 시작하고 대시보드를 관찰한다 — 이게 비교 기준이다.
2. 펌프보다 빠른 주기로 `0x200` 을 위조해 넣는다. 값 배치는
   `u16 rate | u16 infused | u16 remaining | u8 batt | u8 alarm`.
3. 잔량 필드를 조작해 본다. 펌프 쪽 계산은 `vtbi - infused` 를 16비트로 하므로
   `infused > vtbi` 면 되돌아 감긴다 — 화면에 거대한 잔량이 뜬다.
4. 알람 코드를 0 으로 유지해 경보를 지운다. `0x201` 알람 이벤트도 코드 0 으로
   덮어 본다.
5. 반대로 `0x202` 상태 프레임을 위조해 DERS 를 꺼진 상태로 표시하거나,
   `hard_max` 를 65535 로 올려 놓고 처방 화면의 판정을 통과시킨다.

**도구.** CAN 테스트 툴(spec-air-can)로 하는 게 가장 빠릅니다.
Inject → Quick Send 로 한 발씩, Transmit List 로 주기 전송, Fuzz 로 연속 변형.
펌프 행세를 통째로 맡기려면 `pump_sim.py --spoof <mode>`:

| 모드 | 성격 |
|---|---|
| `flatline` | 주입 중인데 화면은 "대기 0 mL/h" — 은폐형 |
| `spike` | 매 프레임 값이 튄다, DERS 상한선까지 흔든다 — 파괴형 |
| `backwards` | 누적 주입량이 줄어든다 — 불가능한 값 |
| `alarmstorm` | 알람 코드 난사 — 진짜 알람이 로그에 묻힌다 |
| `ghost` | 4 프레임에 1 번만 거짓말 — 발견이 가장 늦다 |

단계별 화면 → **`spec-air-can/docs/injection-lab.md`**

**쓰는 결함**: 취약점 21, 2, 3.

**확인**: 대시보드 표시와 실제 버스 상태가 갈라지는가. 임상 사용자가
조작을 알아챌 수 있는 단서가 화면에 남는가.

> 실측 결과 하나: `0x200` 의 바이트 0~5 를 초당 5 회 무작위로 흔들면 대시보드가
> **52,057 mL/h · 잔량 35,201 mL · 배터리 0 %** 를 아무 경고 없이 그리고,
> 그 아래 가드레일은 **"상태: 작동 중 · 절대 상한: 999 mL/h"** 라고 표시합니다.
> DERS 가 *운영자 입력*만 검사하고 *수신 텔레메트리*는 전혀 검사하지 않기
> 때문입니다. **인증은 못 해도 검증은 할 수 있었다** — 이 시나리오의 결론입니다.

---

## 시나리오 3 — 용량 조작 (0x100 스푸핑 · 가드레일 우회)

**노리는 것**: 가드레일을 통과시키고 위험한 주입 속도를 지시한다.

세 갈래가 있습니다. 각각이 다른 결함을 씁니다.

**(a) 제어 포트로 직접**
```
{"op":"setrate","rate":999,"vtbi":500,"drug":2,"weight":70}
```
게이트웨이는 판정 결과와 무관하게 `0x100` 프레임을 내보냅니다 (취약점 3).
화면 판정과 실제 송신이 어떻게 다른지 확인하세요.

**(b) 버스에 프레임 직접 주입**
`0x100` 프레임을 만들어 펌프에 직접 보냅니다. 펌웨어 v0.0.1 은
`rate > 1000` 이면 DERS 를 끄고, `rate == 666` 이면 안전 인터록을 내립니다.

테스트 툴에서는 Inject → Quick Send 에 ID `100`, Data
`FA 00 F4 01 01 00 46 00` (rate 250 · vtbi 500 · drug 1 · weight 70) 형태로 넣고,
이어서 ID `110`, Data `01 01 00 00 00 00 00 00` (CMD_START, token `0x01`) 을 보냅니다.
토큰은 8 비트뿐이고 검증도 없으므로, 앱의 하트비트 `0x110 05 01 …` 를 한 번만
캡처하면 그대로 재사용할 수 있습니다.

**(c) 워크스테이션의 안전 상태를 메모리에서 바꾸기**
`CHEAT_ENGINE_LAB.md` 시나리오 B/C — 상한 값을 올리거나 인터록 플래그를 내립니다.

추가로 볼루스 경로를 보세요. `{"op":"control","cmd":3,"token":255,"arg":65535}` —
토큰 `0x00`/`0xFF` 는 펌웨어에서 권한을 관리자로 올립니다.

**쓰는 결함**: 취약점 3, 9, 21, 2 + 펌웨어 쪽 DERS 결함.

**확인**: 어느 갈래가 화면에 흔적을 남기고 어느 갈래가 남기지 않는가.

---

## 시나리오 4 — 알람 무음화 (리플레이)

**노리는 것**: 폐색이나 주입 완료 알람이 임상 사용자에게 닿지 않게 한다.

`0x101` ALARM_ACK 에는 시퀀스 번호도 논스도 없습니다.

1. 정상 알람 확인 프레임을 한 번 캡처한다 (앱에서 "현재 알람 확인" 을 누른다).
2. 같은 프레임을 주기적으로 재전송한다. 알람이 뜨자마자 지워진다.
3. 워크스테이션 쪽에서도 같은 효과를 낸다 — `0x201` 을 코드 0 으로 위조하거나,
   `0x200` 의 알람 바이트를 0 으로 유지한다.
4. 앱의 알람 화면 목록과 `runtime/logs/cannula.log` 를 비교한다.

**쓰는 결함**: 취약점 21 (리플레이 방지 없음), 2.

**확인**: 알람이 발생했다는 증거가 어디에 남는가. 남지 않게 만들 수 있는가.

---

## 시나리오 5 — 변조된 CAN 프레임으로 워크스테이션에서 코드 실행 ⭐

**노리는 것**: 버스에 프레임을 흘리는 것만으로 워크스테이션 프로세스를 장악한다.

이 랩의 핵심 시나리오입니다. 두 경로가 있고, 둘 다 "펌프가 보낸 데이터"에서
출발합니다.

### 5a. 렌더러 경로 (XSS → RCE)

워크스테이션은 `0x203` 프레임의 ASCII 를 이어 붙여 서비스 응답 문자열을 만들고,
그것을 **`innerHTML` 로** 화면에 넣습니다. 렌더러는 Node 권한을 갖고 있습니다.

1. `0x203` 프레임의 첫 바이트가 `0x00` 이면 재조립 버퍼가 초기화된다는 것을 확인한다.
2. 그 뒤로 8바이트씩 프레임을 보내 원하는 문자열을 조립한다.
3. HTML 요소로 파싱되는 문자열을 만들어 본다. 렌더러에서 `require` 가 되는지 확인한다.
4. 같은 성질의 다른 싱크도 확인한다 — 알람 배너(`0x201` 의 뒤쪽 6바이트),
   버스 트래픽 표의 "해석" 열(`decodeFrame()` 이 프레임 바이트를 ASCII 로 넣는다),
   알람 표의 장치 문자열 열.

**쓰는 결함**: 취약점 13 + 12.

### 5b. 네이티브 경로 (핸들러 테이블 덮어쓰기)

`0x203` 재조립 버퍼 바로 뒤에 **쓰기 위치 자신**과 **프레임 핸들러 함수 포인터
테이블**이 놓여 있습니다.

```
can_ctx_t  +0x00  char  resp_buf[64]
           +0x40  u32   resp_off      ← 64바이트를 넘긴 쓰기가 여기 닿는다
           +0x44  u32   resp_len
           +0x48  ptr   handlers[12]  ← 프레임마다 호출된다
```

복사 길이와 쓰기 위치에 상한 검사가 없습니다.

1. `0x203` 프레임 하나(`data[0]=0x00`)로 버퍼를 초기화한다.
2. 8바이트 프레임 8개를 보내 `resp_off` 를 64로 만든다.
3. 다음 프레임이 `resp_off` 자체를 덮는다 — **쓰기 위치를 여러분이 정한다.**
   값을 정할 때 `resp_off += dlc` 가 뒤따르는 것을 계산에 넣어야 한다.
4. 그 다음 프레임이 `handlers[N]` 에 들어간다. 어떤 프레임 ID 가 몇 번째 핸들러를
   쓰는지는 등록 순서를 보면 안다 (`can_engine_init`: 0x200, 0x201, 0x202, 0x203, 0x300).
5. 해당 ID 의 프레임을 하나 보내면 그 주소로 제어가 넘어간다.

ASLR 도 DEP 도 없습니다 (취약점 8). 즉 **주소를 고정으로 계산할 수 있고,
데이터 영역에 올린 코드가 그대로 실행됩니다.** Cheat Engine 으로 실행 페이지를
할당해 두고 그 주소를 프레임으로 써넣는 조합이 자연스럽습니다 —
`CHEAT_ENGINE_LAB.md` 시나리오 D.

**쓰는 결함**: 취약점 1 + 8 + 21.

**확인**: 프레임 몇 개로 제어 흐름을 가져올 수 있는가. 앱 화면에는 무엇이 보이는가
(아무것도 안 보인다면 그것이 이 시나리오의 요점이다).

---

## 시나리오 6 — 장치 식별자를 경로로 (임의 파일 쓰기)

**노리는 것**: 워크스테이션이 스스로 파일을 쓰게 만들어 지속성을 얻는다.

워크스테이션은 펌프에 `id` 를 물어 받은 응답(`id=...`)을 장치 식별자로 보관하고,
**세션 로그 파일 이름에 그대로 씁니다.** 정규화도 경계 확인도 없습니다.

1. 앱의 서비스 콘솔에서 `id` 를 보내 정상 응답 흐름을 관찰한다
   (`0x203` 로 `id=CANNULA-PUMP-01` 이 돌아온다).
2. 응답을 위조해 식별자에 경로 구분자를 넣는다. 앱의 이벤트 로그 화면에서
   기본 파일 이름이 바뀌는 것을 확인한다.
3. 어디에 쓸 수 있는지 생각한다. 앱은 사용자 폴더 설치가 기본이므로 권한 상승 없이
   닿는 곳이 많다 — 시작 폴더, 앱의 `plugins\` 폴더(취약점 17!), 게이트웨이 폴더의
   DLL 이름(취약점 11!).
4. 저장되는 CSV 의 구조를 본다. 헤더 몇 줄은 `#` 로 시작하고, 본문은
   `시각,구분,"내용"` 이다. 내용은 앱을 쓰면서 여러분이 채울 수 있다.

**쓰는 결함**: 취약점 14 → 17 / 11 로 연결.

**확인**: 파일 쓰기 하나를 코드 실행으로 승격시키는 가장 짧은 경로는 무엇인가.

---

## 시나리오 7 — 제어 포트로 원격 장악

**노리는 것**: 워크스테이션과 같은 네트워크에서 사전 인증 없이 코드를 실행한다.

제어 포트는 인증이 없고 `0.0.0.0` 에 열려 있습니다 (취약점 9). 그리고 명령을
파싱하는 `jget_str()` 은 JSON 문자열 값을 **길이 제한 없이** 고정 크기 스택
버퍼에 옮깁니다 (취약점 10).

호출부의 버퍼 크기:

| 명령 | 필드 | 버퍼 |
|---|---|---|
| `tx` | `data` | `char hex[64]` |
| `debug` | `cmd`, `args` | `char cmd[64]`, `char args[64]` |
| `override` | `code` | `char code[64]` |
| `transport` | `mode`, `serial` | `char mode[32]`, `char port[64]` |

UI 줄 버퍼는 4096바이트입니다.

1. 짧은 값으로 정상 동작을 확인한다.
2. 값을 길게 늘려 가며 게이트웨이의 반응을 본다. 어느 길이에서 무엇이 깨지는가.
3. 스택 레이아웃을 확인한다. `-fno-stack-protector` 로 빌드되어 카나리가 없고,
   `--disable-dynamicbase` 로 이미지 베이스가 고정
   (`CANnulaBridge.exe` → `0x140000000`)이다.
4. `--disable-nxcompat` 이므로 데이터 영역의 코드가 실행된다는 점을 계산에 넣는다.

디버깅에는 `x64dbg` 나 WinDbg 를 붙이고, 오프셋 확인은 Cheat Engine 의
메모리 뷰어가 편합니다.

**쓰는 결함**: 취약점 9 + 10 + 8.

**확인**: 원격에서 어디까지 갈 수 있는가. 이 경로를 막는 최소 수정은 무엇인가.

---

## 시나리오 8 — 권한 상승 (기사·관리자 기능)

**노리는 것**: 간호사 계정으로 로그인한 상태에서 서비스 기능을 연다.

앱의 역할 분리는 **메뉴 버튼을 숨기는 것**뿐입니다.

1. `nurse.kim` 으로 로그인한다. 진단·펌웨어 메뉴가 보이지 않는 것을 확인한다.
2. 무엇이 실제로 막혀 있는지 확인한다. 메인 프로세스의 IPC 핸들러가 역할을 보는가?
3. 세션이 어디에 어떤 형식으로 저장되는지 본다 (`localStorage`).
4. 설정 파일 `[users]` 를 읽어 다른 계정을 확보한다. 로그인 화면이 이미 사용자
   이름을 알려 주고 있다.
5. 네이티브 쪽 `auth_level` (구조체 `+0x1B`) 경로도 있다 —
   `CHEAT_ENGINE_LAB.md` 시나리오 C.

**쓰는 결함**: 취약점 19, 12.

**확인**: 클라이언트 측 권한 판정이 무엇을 막고 무엇을 막지 못하는가.

---

## 시나리오 9 — 공급망: 플러그인 · DLL · 업데이터

**노리는 것**: 앱 기동이나 업데이트 시점에 코드를 실행한다.

세 경로 모두 서명 검증이 없습니다.

**(a) 플러그인** — `runtime/plugins/*.js` 는 시작할 때 전부 `require()` 됩니다.
메인 프로세스 권한으로 돕니다. `site-drug-labels.js` 를 읽어 인터페이스를 파악하세요.

**(b) 엔진 DLL** — `CANnulaBridge.exe` 는 `cannula_can.dll` 을 이름으로 임포트하고
자기 폴더를 작업 디렉터리로 실행됩니다. 내보내는 함수 목록은 `native/cannula_can.h`
에 있습니다. 정상 기능을 유지하면서 코드를 덧붙이려면 어떤 함수를 감싸야 할까요.

**(c) 업데이터** — 매니페스트를 평문 HTTP 로 받고(`[update] base_url`),
`artifactUrl` 의 내용을 서명 확인 없이 Node 로 실행합니다. TLS 를 써도
인증서를 검증하지 않습니다. 매니페스트 형식은 앱의 업데이트 화면에서 확인하세요
(`artifactUrl` 필드가 있어야 적용 버튼이 열립니다).

**쓰는 결함**: 취약점 17, 11, 16, 18.

**확인**: 세 경로 중 어느 것이 가장 조용한가. 탐지하려면 무엇을 봐야 하는가.

---

## 시나리오 10 — 정보 수집: 펌프 메모리와 로그

**노리는 것**: 펌프와 워크스테이션에서 쓸 만한 값을 모은다.

1. 앱의 **진단 → 메모리 조회** 로 `0x08000000` 부터 읽어 본다.
   펌프는 `0x110` CMD `0x06` 을 받으면 `0x300` 프레임으로 바이트를 돌려주고,
   워크스테이션은 그것을 hex+ASCII 로 보여 준다. 접근 제어가 없다.
2. 알람 이벤트(`0x201`)의 뒤쪽 6바이트를 보라. 펌웨어 v0.0.1 은 여기에 내부
   문자열을 넣는다. 버스 트래픽 표의 `tag=` 값과 앱 로그를 확인한다.
3. 서비스 콘솔의 `dump`, `get`, `mem <주소>` 응답을 본다.
4. 워크스테이션 쪽 수집물: `config\cannula.ini` (계정·해제 코드·업데이트 서버),
   `logs\cannula.log` 와 `logs\*-events.csv` (환자 이름·ID·병상·체중·알레르기,
   입력된 해제 코드).
5. 게이트웨이 프로세스 메모리도 대상이다 — 서비스 비밀번호와 해제 코드가
   `.data` 에 문자열로 있다 (`strings` 로도 보인다).

**쓰는 결함**: 취약점 20, 19, 4, 21 + 펌웨어 쪽 메모리 접근 제어 부재.

**확인**: 모은 값 중 어느 것이 다른 시나리오의 입력이 되는가.

---

## 시나리오 11 — DoS

**노리는 것**: 임상 사용자가 펌프를 볼 수 없게 만든다.

1. **범위 밖 인덱스**: 텔레메트리의 `drug_id` 나 `alarm_code` 를 테이블 크기보다
   큰 값으로 보낸다. 워크스테이션은 그 값을 배열 인덱스로 그대로 쓴다 (취약점 4).
   프레임 하나로 어디까지 되는가.
2. **체중 0**: 용량 계산의 나눗셈을 본다 (취약점 5).
3. **버스 플러딩**: 낮은 ID 로 프레임을 쏟아 아비트레이션을 독점한다.
   워크스테이션 UI 는 어떻게 되는가 (버스 트래픽 표는 400 프레임을 들고 있다).
4. **제어 포트**: 8개 슬롯을 모두 점유하면 정상 UI 가 붙을 수 있는가.
5. **DLC**: UDP 로 직접 주입할 때 `dlc` 를 8보다 크게 준다. 게이트웨이와 엔진이
   이 값을 어디에서 쓰는지 추적한다.

**쓰는 결함**: 취약점 4, 5, 1, 9, 21.

**확인**: 어느 DoS 가 임상적으로 가장 위험한가 — 앱이 죽는 것과 앱이 살아서
거짓말하는 것 중 무엇이 더 나쁜가.

---

## 방어 관점 정리 (실습 마무리)

각 시나리오를 막는 최소 수정을 직접 써 보세요. 참고 축:

| 층 | 조치 |
|---|---|
| CAN | 프레임 MAC + 단조 카운터, 검증 실패 프레임 폐기·기록 |
| 게이트웨이 | `127.0.0.1` 바인드, 로컬 토큰 인증, 길이 제한 파서, 하드닝 켜기(`/GS /DYNAMICBASE /NXCOMPAT`) |
| 네이티브 상태 | 안전 판정 값과 표시 값 분리, 이중 저장·체크섬, `switch` 디스패치 |
| Electron | `contextIsolation` + `sandbox`, `textContent`, CSP, IPC 마다 권한 검사 |
| 파일 경로 | `basename` + 경계 확인, 장치 문자열 화이트리스트 |
| 배포 | 코드 서명 검증(플러그인·DLL·업데이트), HTTPS 고정 + 인증서 검증 |
| 자격 | 서버 측 인증, 비밀번호 해시, 서명된 세션 토큰 |
| 로그 | 개인정보 가명화, 자격증명 미기록, 접근 제어 |

## 참고

- 취약점 코드 위치: `VULNERABILITIES.md`
- 메모리 조작·코드 주입: `CHEAT_ENGINE_LAB.md`
- 펌프 쪽: `../ptl-weak-cannular-fw/HACKING_SCENARIOS.md`
- 도구: `can-utils`, SavvyCAN, `python-can`, `scapy`, Cheat Engine, x64dbg,
  Ghidra, Wireshark, mitmproxy
