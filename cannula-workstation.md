# CANnula Clinical Workstation — 설계 메모

`ptl-weak-cannular-fw` 의 `cannular.md` 에 대응하는 데스크톱(워크스테이션) 쪽
설계 문서입니다. **왜 이렇게 만들었는가**를 남겨 둡니다.

## 1. 위치

```
[데스크톱: 임상 워크스테이션]  ← 이 저장소
        │  USB-CAN 어댑터 (CANable/candleLight) 또는 가상 CAN
     ── CAN 버스 (양 끝 120Ω 종단) ──
        │  트랜시버 SN65HVD230
[임베디드: 펌프 컨트롤러]  STM32F103C8T6 + 펌웨어 v0.0.1  ← ptl-weak-cannular-fw
```

`cannular.md` 는 데스크톱을 Python/Qt + SocketCAN 으로 잡았지만, 실습 목표가
**Windows 데스크톱 앱 침투 + Cheat Engine 메모리 조작**이므로 다음으로 바꿨습니다.

- **UI**: Electron — 형제 프로젝트 `ptl-weak-simple-pacs-win` 과 같은 스택.
  Electron 특유의 공격 표면(렌더러 Node 권한, `innerHTML` 싱크, 무검증 업데이터,
  서명 없는 플러그인)을 그대로 쓸 수 있다.
- **CAN + 안전 상태**: 네이티브 C (`cannula_can.dll` + `CANnulaBridge.exe`).

## 2. 왜 네이티브 계층을 따로 두었나

Cheat Engine 실습이 목표라면 대상이 **네이티브 프로세스**여야 합니다.

| | V8 힙 (Electron 만) | 네이티브 전역 구조체 |
|---|---|---|
| 주소 안정성 | GC 로 움직인다 | ASLR 끄면 고정 |
| 값 고정(freeze) | 잘 안 듣는다 | 그대로 듣는다 |
| 구조 파악 | 태그된 포인터·인라인 캐시 | 오프셋 표 하나로 끝 |
| 코드 주입 | JIT 영역, 재현 어려움 | 함수 포인터 하나 |
| 교육 효과 | "V8 내부구조 수업" 이 되어 버린다 | "안전 판정을 조작 가능한 곳에 두면 안 된다" 가 남는다 |

그리고 이 분리는 **제품으로서도 자연스럽습니다**. 실제 제품에서도 어댑터를
붙들고 프레임을 타이밍 맞춰 처리하는 일은 별도 서비스가 맡고, UI 는 그 위에
얹습니다. 그래서 "실습을 위해 억지로 만든 구조" 로 보이지 않습니다.

경계는 이렇게 그었습니다.

| 계층 | 담당 |
|---|---|
| `cannula_can.dll` | 프레임 조립/해석, 펌프 표시 상태, DERS 판정, 용량 계산, 펌프 시뮬레이터 |
| `CANnulaBridge.exe` | 전송(sim/slcan/udp), UI 용 TCP/JSON 서버, 프레임 로그 |
| `main.js` | 게이트웨이 기동·연결, 설정/플러그인/로그, 파일·업데이트·진단 도구 |
| `renderer/index.html` | 화면 전체, 로그인, 이벤트 로그, 펌웨어 청크 분할 |

## 3. 하드웨어 없이 돌아가야 한다

강의실에서 STM32 + USB-CAN 어댑터를 인원수만큼 준비할 수 없습니다.
그래서 게이트웨이 안에 **펌프 시뮬레이터**를 넣었습니다. 기본 전송은 실제
하드웨어(`slcan`) 이고, 어댑터가 없는 자리에서 `sim` 으로 바꿔 씁니다.

시뮬레이터는 펌웨어 v0.0.1 의 동작을 그대로 흉내냅니다 — 검증 누락과 백도어까지:

- `rate > 1000` → DERS 해제 (`CAN_HandleSetRate`)
- `rate == 666` → 안전 인터록 해제 (`Pump_SetRate`)
- 제어 토큰 `0x00`/`0xFF` → 권한 관리자로 상승 (`CAN_HandleControl`)
- `0x120` AUTH_REQUEST → 요청만으로 관리자 (`CAN_ProcessMessage`)
- `0x201` 알람 이벤트 뒤쪽 6바이트에 서비스 비밀번호 (`CAN_SendAlarm`)
- 서비스 콘솔 `unlock` / `dump` / `set auth` (`ProcessDebugCommand`)
- 펌웨어 업데이트 청크 수신 시 서명 검증 없이 안전 기능 해제
- `0x666` 미문서 ID

이렇게 해 두면 하드웨어 실습과 시뮬레이터 실습의 **결과가 같습니다.**
펌웨어 문서의 시나리오를 그대로 따라할 수 있습니다.

세 전송 방식의 역할 분담:

| 전송 | 용도 |
|---|---|
| `slcan` | 기본. 실제 펌프. USB-CAN 어댑터를 COM 포트로 직접 제어 |
| `sim` | 하드웨어 없는 실습. 시뮬레이터가 펌프 역할 |
| `udp` | 프레임 주입 실습. `tools/vcan_bridge.py` 로 can-utils·SavvyCAN·python-can 연결. 시뮬레이터는 기본으로 꺼진다 (외부 소스가 유일한 발신자) |

`serialport` 같은 네이티브 npm 모듈을 쓰지 않고 게이트웨이 C 코드에서 직접
Win32 `CreateFile(\\.\COMx)` 를 쓴 이유: (a) Electron 리빌드 의존을 없애고,
(b) 프레임 파싱을 네이티브 쪽에 모아 Cheat Engine 대상을 한 곳으로 만들기 위해.

## 4. 취약점을 어떻게 심었나 — 원칙

형제 프로젝트와 같은 원칙을 지켰습니다.

**"제품처럼 생긴 대상"이지, 공격 시연 장치가 아니다.**

- 앱 안에 공격을 대신 실행해 주는 버튼이 없다.
- 미리 채워 둔 페이로드가 없다.
- 취약점은 전부 **그럴 법한 제품 기능** 안에 들어 있다.

각 취약점이 어떤 "제품 요구사항" 으로 위장되어 있는지:

| 취약점 | 제품 쪽 명분 |
|---|---|
| 제어 포트 `0.0.0.0` 바인드 | "옆 병동 워크스테이션에서도 같은 펌프를 보게" |
| 장치 식별자로 로그 파일 이름 구성 | "병원에서 펌프별로 로그를 모아 둔다" |
| 진단 도구 인자 이어붙이기 | "서비스 기사가 현장에서 옵션을 덧붙여 쓴다" |
| 서명 없는 플러그인 로드 | "사이트마다 약물 표기가 달라 본체 재배포 없이 바꾼다" |
| 설정 파일의 실행 파일 경로 | "게이트웨이를 다른 위치에 두는 사이트가 있다" |
| 평문 계정 파일 | "병동 인수인계용 공용 계정" |
| 표시 속도 필드 분리 | "화면 갱신과 프레임 처리를 분리" |
| 함수 포인터 디스패치 테이블 | 흔한 C 설계 관용구 |
| 자가진단이 구조체 주소 출력 | "지원팀이 메모리 배치를 확인" |
| 보호 기능 끈 빌드 | `-O0 -g` 디버그 빌드가 그대로 출하된 모양 |

**계층별로 골고루** 심었습니다. 한 계층만 보면 안 된다는 것을 배우게 하려는
의도입니다.

| 계층 | 취약점 수 |
|---|---|
| 네이티브 (메모리 안전·계산·빌드) | 8 |
| 게이트웨이 (네트워크·파싱·로드) | 3 |
| Electron (렌더링·파일·프로세스·배포) | 7 |
| 자격·데이터 | 2 |
| 프로토콜 | 1 |

## 5. 핵심 체인 (설계 의도)

이 랩에서 가장 보여 주고 싶은 것은 **"버스에 프레임을 흘리는 것만으로
워크스테이션을 장악한다"** 입니다. 그래서 다음 체인이 성립하도록 배치했습니다.

```
CAN 0x203 프레임  (인증 없음, 길이 무검증)
      │
      ├─→ 재조립 문자열 → innerHTML → 렌더러 RCE (Node 권한)
      │
      ├─→ 재조립 문자열 → 장치 식별자 → 로그 파일 경로 → 임의 파일 쓰기
      │                                        └→ plugins\*.js / cannula_can.dll 로 지속성
      │
      └─→ 버퍼 오버플로 → resp_off 덮기 → handlers[] 덮기 → 제어 흐름 탈취
                                                    (ASLR/DEP 없음)
```

`can_ctx_t` 의 필드 순서(`resp_buf[64]` → `resp_off` → `handlers[]`)는
**의도적으로 그렇게 배치했습니다.** 별개의 전역 변수로 두면 링커가 순서를
바꿀 수 있어 실습 자료의 오프셋이 어긋납니다. 하나의 구조체에 넣어 인접성을
보장했습니다.

`resp_off` 가 버퍼와 테이블 **사이에** 있는 것도 의도입니다. 단순한 선형
오버플로가 아니라 **쓰기 위치를 공격자가 정하는** 단계를 거치게 만들어,
"오프셋을 계산해야 하는" 실습이 되게 했습니다.

## 6. 실습 난이도 순서 (권장)

| 단계 | 시나리오 | 필요한 도구 |
|---|---|---|
| 기초 | 버스 관찰, 제어 포트 정찰, 설정·로그 수집 | candump, nc, 텍스트 편집기 |
| 기초 | 텔레메트리·상태 위조로 화면 속이기 | python 소켓 |
| 중급 | 0x100 스푸핑 / 알람 리플레이 / 가드레일 우회 | python-can |
| 중급 | 클라이언트 측 권한 우회, 평문 자격증명 | 개발자 도구 |
| 중급 | Cheat Engine 값 스캔·freeze (표시 조작) | Cheat Engine |
| 고급 | 0x203 → XSS → RCE | 프레임 조립 스크립트 |
| 고급 | 장치 식별자 경로 순회 → 지속성 | 위 + 파일 시스템 이해 |
| 고급 | 함수 포인터 테이블 덮어쓰기 + 코드 주입 | Cheat Engine + x64dbg |
| 고급 | 제어 포트 원격 스택 오버플로 | x64dbg |
| 마무리 | 각 취약점의 최소 수정 작성 | — |

## 7. 안전 · 윤리

- 격리된 랩에서만. 실제 의료기기·환자·병원 네트워크 금지.
- 주입은 시뮬레이션과 LED 로만 표현한다 — 물리적 위험 0.
- 목적은 방어·교육. 취약점은 학습용으로 의도적으로 심은 것.
- 발견한 취약점은 책임 있게 보고하는 절차를 함께 연습한다.

## 8. 관련 문서

| 문서 | 내용 |
|---|---|
| `README.md` | 제품 소개, 실행 방법, 화면 구성, CAN 메시지셋 |
| `VULNERABILITIES.md` | 취약점 21건 — 코드 위치, CWE, 원인, 공격 경로, 수정 |
| `HACKING_SCENARIOS.md` | 침투 시나리오 11건 |
| `CHEAT_ENGINE_LAB.md` | 메모리 조작·코드 주입 실습 (오프셋 표, AA 스크립트) |
| `tools/README.md` | 랩 배선 (vcan ↔ UDP, slcan 하드웨어) |
| `../ptl-weak-cannular-fw/` | 펌프 펌웨어와 그쪽 취약점·시나리오 |
